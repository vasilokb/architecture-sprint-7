apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: isolate-admin-back-end-api
  namespace: system
spec:
  podSelector:
    matchLabels:
      role: admin-back-end-api
  policyTypes:
  - Ingress
  - Egress
  ingress: []
  egress: []